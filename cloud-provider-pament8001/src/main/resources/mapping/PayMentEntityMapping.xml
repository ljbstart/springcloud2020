<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.atguigu.mysql.mapper.PaMentEntityMapper" >
    <resultMap id="BaseResultMap" type="com.atguigu.mysql.entity.PayMentEntity">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="serial" property="serial" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="getPayMentById" resultMap="BaseResultMap" parameterType="java.lang.Long">
        SELECT  *
        FROM
        test
        WHERE
        id = #{id,jdbcType=BIGINT}
    </select>

    <insert id="creat" parameterType="com.atguigu.mysql.entity.PayMentEntity" useGeneratedKeys="true" keyProperty="id">
        insert into test
        (id,serial)
        values
        (#{id,jdbcType=BIGINT},#{serial,jdbcType=VARCHAR})
    </insert>

</mapper>